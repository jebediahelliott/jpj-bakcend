{
  "name": "jpj-canine-training",
  "engines": {
    "node": "10.15.3",
    "npm": "6.9.0"
  },
  "scripts": {
    "build": "npm install --prefix ./client && npm run build --prefix ./client",
    "deploy": "cp -a client/build/. public/",
    "set-fallback-route": "cp -a public/index.html app/views/application/index.html.erb",
    "heroku-postbuild": "npm run build && npm run deploy && npm run set-fallback-route"
  }
}
